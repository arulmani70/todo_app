import 'package:flutter/material.dart';

class TodoPagePlaceholder extends StatelessWidget {
  const TodoPagePlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
